{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    ":semanticCommits",
    "security:minimumReleaseAgeNpm",
    "group:definitelyTyped",
    "group:jsUnitTest",
    "group:nextjsMonorepo"
  ],
  "timezone": "Asia/Seoul",
  "schedule": ["before 10am on monday"],
  "prHourlyLimit": 0,
  "prConcurrentLimit": 10,
  "rangeStrategy": "bump",
  "labels": ["dependencies"],
  "lockFileMaintenance": {
    "description": "자동 머지하지 않는다. Renovate의 minimumReleaseAge는 lockFileMaintenance에 적용되지 않는다(전이 의존성 해석을 패키지 매니저에 맡기므로 나이를 볼 릴리스가 없다). 이 PR은 모든 전이 의존성을 방금 나온 버전으로 다시 해석하므로, 나이 게이트 없이 자동 머지하면 공개된 지 몇 분 된 악성 버전도 그대로 들어온다. 게이트는 pnpm-workspace.yaml의 pnpm minimumReleaseAge 몫이다 — 그게 들어오면 automerge를 되살려도 된다",
    "enabled": true,
    "schedule": ["* 0-9 1-3 * *"],
    "automerge": false
  },
  "packageRules": [
    {
      "description": "프리셋에 없는 그룹 — TanStack",
      "matchPackageNames": ["/^@tanstack\\//"],
      "groupName": "tanstack"
    },
    {
      "description": "프리셋에 없는 그룹 — Panda CSS",
      "matchPackageNames": ["/^@pandacss\\//"],
      "groupName": "panda"
    },
    {
      "description": "GitHub Actions는 ci scope로 묶고(기본 scope는 deps), 공개된 지 3일이 지난 릴리스만 올린다. 프리셋 security:minimumReleaseAgeNpm은 npm 데이터소스에만 걸려 액션 업데이트(자동 머지 대상)에는 나이 게이트가 없었다 — 태그가 탈취되거나 악성 릴리스가 나오면 배포·리뷰 잡에서 곧바로 돌았다",
      "matchManagers": ["github-actions"],
      "groupName": "ci",
      "semanticCommitScope": "ci",
      "minimumReleaseAge": "3 days"
    },
    {
      "description": "dependencies·devDependencies 모두 minor/patch까지 자동 머지. 예전에는 프로덕션 dependencies만 patch로 좁혀 뒀는데, 그 결과 프로덕션 minor가 어느 규칙에도 안 걸려 automerge도 deps-major 라벨도 없이 열린 채 쌓였다(lucide-react·mermaid·motion·nuqs·@supabase/postgrest-js·magic-string). 블로그는 build 안에 check-seo·check-bundle까지 게이트가 있어 회귀는 머지 전에 잡힌다. 런타임 자체(.tool-versions의 node·pnpm, engines, packageManager)는 여전히 어느 규칙에도 안 걸려 수동이다 — 자동으로 올리면 asdf에 안 깔린 버전을 가리켜 로컬이 죽는다",
      "matchDepTypes": ["dependencies", "devDependencies"],
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": true,
      "platformAutomerge": true
    },
    {
      "description": "pnpm-workspace.yaml의 overrides·catalog도 minor/patch까지 자동 머지. overrides는 대부분 transitive 의존성의 보안 패치이고, catalog는 전역 고정이라 영향 범위가 넓지만 CI가 lint·types·test를 모두 돌린다. 이 규칙이 없으면 depType이 dependencies/devDependencies 어느 쪽도 아니라 automerge가 전혀 걸리지 않는다",
      "matchDepTypes": ["pnpm-workspace.overrides", "/^pnpm\\.catalog\\./"],
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": true,
      "platformAutomerge": true
    },
    {
      "description": "@types/*는 타입 정의뿐이라 minor까지 자동 머지",
      "matchPackageNames": ["/^@types\\//"],
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": true,
      "platformAutomerge": true
    },
    {
      "description": "GitHub Actions의 minor/patch도 자동 머지",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": true,
      "platformAutomerge": true
    },
    {
      "description": "anthropics/claude-code-action은 SHA로 고정하고 자동 머지하지 않는다. CLAUDE_CODE_OAUTH_TOKEN과 (github_token을 넘기지 않으면) 잡 권한과 무관하게 write로 발급되는 App 토큰을 받는 액션이고, 이 저장소는 액션 동작 변화(에이전트 모드에서 게시 누락 등)에 여러 번 데였다. 매 버전을 diff로 보고 사람이 머지한다. ci 그룹에서 떼어 따로 묶는다 — 그룹 브랜치는 안의 업데이트가 전부 automerge를 허용해야 자동 머지되므로, 자주 나오는 이 액션이 ci 그룹에 남으면 checkout·setup-node 같은 나머지 액션의 자동 머지까지 멈춘다",
      "matchPackageNames": ["anthropics/claude-code-action"],
      "groupName": "claude-code-action",
      "automerge": false
    },
    {
      "description": "@ssgoi/react는 업데이트 제안을 막는다. v7이 PageTransition의 API(paths·preserveScroll, AnyTransitionConfig 형태 등)와 비호환이라 마이그레이션 없이는 빌드가 깨진다(#246). 마이그레이션을 마치면 이 규칙을 지울 것",
      "matchPackageNames": ["@ssgoi/react"],
      "enabled": false
    },
    {
      "description": "typescript major(v7)는 제안을 막는다. 이 저장소는 TS6 시맨틱을 전제로 tsconfig와 소스를 맞춰 뒀고(AGENTS.md), v7은 별도 마이그레이션 작업이다. PR #244를 머지 없이 닫아 Renovate가 이미 7.x를 암묵적으로 무시하고 있지만, 대시보드의 recreate 체크박스 한 번이면 되살아나고 사유가 코드에 남지 않는다. major만 막으므로 6.x의 minor/patch는 계속 올라온다. 마이그레이션을 마치면 이 규칙을 지울 것",
      "matchPackageNames": ["typescript"],
      "matchUpdateTypes": ["major"],
      "enabled": false
    },
    {
      "description": "major는 자동 머지하지 않고 deps-major 라벨을 붙인다 — 이 라벨이 붙은 PR만 claude-code-review.yml이 리뷰한다",
      "matchUpdateTypes": ["major"],
      "addLabels": ["deps-major"],
      "automerge": false
    }
  ]
}
