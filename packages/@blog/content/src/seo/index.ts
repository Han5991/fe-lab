/**
 * @blog/content 공개 API — 문(door) 2: SEO 빌더.
 *
 * 포스트 → SEO 메타데이터(PostSeoData DTO) · JSON-LD의 순수 계산.
 * 프레임워크 중립이다 — Next `Metadata`로의 어댑트는 앱(nextMetadata.ts)이 한다.
 */
export * from './postSeo.ts';
