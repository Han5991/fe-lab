{
  "name": "typescript",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "tsc -watch",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=24.20.0"
  },
  "devDependencies": {
    "@package/config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
