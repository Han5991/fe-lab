{
  "name": "typescript",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "tsc -watch",
    "build": "tsc --noEmit",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=24.19.0"
  },
  "devDependencies": {
    "@package/config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
