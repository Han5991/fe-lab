{
  "name": "typescript",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "tsc -watch",
    "build": "tsc --noEmit",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=24.18.1"
  },
  "devDependencies": {
    "@package/config": "workspace:*",
    "@package/core": "workspace:*",
    "msw": "^2.15.0",
    "typescript": "catalog:",
    "vitest": "^4.0.0-beta.15"
  }
}
