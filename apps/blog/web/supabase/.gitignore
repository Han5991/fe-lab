# Supabase
.branches
.temp

# 프로덕션 DB 의 데이터 덤프 — 원본은 DB 이고 이 파일은 그 사본이다.
# 저장소가 들고 있을 이유가 없다(조회 로그라 계속 자라기만 하고, 커밋 시점마다
# 몇천 줄짜리 diff 가 생긴다). 필요하면 `pnpm seed:pull` 로 언제든 다시 만든다.
# 없어도 `supabase db reset` 은 경고만 내고 지나간다.
seed.sql

# dotenvx
.env.keys
.env.local
.env.*.local
